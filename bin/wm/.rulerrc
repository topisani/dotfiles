# -*- mode: conf; tab-width: 4 -*-
#
# ╔═║║ ║║  ╔═╝╔═╝
# ╔╔╝║ ║║  ╔═╝══║
# ╝ ╝══╝══╝══╝══╝
# move all browsers to workspace 2
type="dock"
    ignw -s $RULER_WID; wmcl desktop move 0 $RULER_WID
class="Polybar"
    ignw -s $RULER_WID; wmcl desktop move 0 $RULER_WID
type="normal"
    wmcl desktop assign $RULER_WID; wmcl focus $RULER_WID
type="dialog"
    wmcl desktop assign $RULER_WID; wmcl focus $RULER_WID
